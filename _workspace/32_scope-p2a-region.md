# 스코프 확장 Phase 2A — 지역 코드 17 시·도 매핑

날짜: 2026-06-28 · 순수 로직(재인제스트·캐시 무변경) · TDD

## 근거(실데이터)
- 실 온통 zipCd 2자리 prefix 샘플 → 17개 확정: 11,26,27,28,29,30,31,36,41,43,44,46,47,48,50,**51(강원특자),52(전북특자)**.
  (42/45 없음 → 신규 특별자치도 코드 사용 확인. 추측 아닌 실측.)

## 변경
- **신규 `src/domain/parse/sido.ts`**: 17 시·도 {code,name,re}. `sidoNameByPrefix(prefix)`.
- **`parseRegion`**: 서울만 → **17 시·도 전면 식별**(다수 시·도 동시 명시 모두 수집). 불명≠전국 보수 유지.
- **`deriveRegionText`(어댑터)**: zipCd prefix → 시·도 명칭 join(매핑 불가/없음 → 기관명 폴백).
- **regionAxis**: 기존 시·도 정확매칭(codes.includes(userCode))으로 충분 — 무변경. '단계적 노출'은
  사용자 시·도 외 정책이 자연히 blocked→미노출되어 달성(프로필 지역은 2C).

## 안전
- 불명 지역 → 빈 코드(보수), 불명≠전국. 광주(29)/경기(41), 충북/충남, 전북/전남 충돌 가드(테스트).
- 캐시 무변경 → 현재 동작 회귀 0(2B 재인제스트 시 신규 매핑 적용).

## 검증
- 테스트 **587 passed (35 files)** · tsc 0. region.test(17 시·도·충돌·전국·불명), deriveRegionText(zipCd→명칭).

## 다음 (2B/2C)
- **2B**: 인제스트 서울필터 제거 + **임베딩 precompute**(CachedPolicy에 vector 저장, ingest 시 1회 계산,
  app은 저장 벡터 로드 → 매 로드 재임베딩 0) + 재인제스트(전국·Gemini 임베딩). 캐시 스키마·파이프라인 변경 → 별도 집중.
- **2C**: 프로필 시·도 선택 UI(하드코딩 11 제거).
