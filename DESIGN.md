# DESIGN.md — 청년정책 진단 디자인 SSOT

> 이 문서는 UI 작업의 단일 진실 원천이다. 모든 UI 구현·수정(Phase D·E·F 포함)은 이 문서를 근거로 한다.
> 문서와 코드가 어긋나면 **이 문서를 먼저 고치고** 코드를 따라가게 한다.
> 확정 근거: 2026-07-02 사용자 승인 시안 2종(메인 화면·펼침 카드) + PLAN_ops-and-seoul-expansion.md Phase E·F.

## 1. 원칙

1. **다정함이지 발랄함이 아니다.** 핵심 사용자는 지치고 무기력한 상태로 들어온다. 통통 튀는 모션·과한 이모지·명랑한 감탄은 부담이다. 차분한 온기.
2. **안전 표면은 톤 전환 금지.** 위기 배너(SafetyBanner)·위기 푸터(CrisisFooter)·"추정" 고지(DisclaimerNote)·원문 링크는 진지한 문구·형태를 유지한다. 여기가 흔들리면 신뢰가 무너진다.
3. **개수와 사실로 신뢰를 만든다.** "N개를 찾았어요", "어제 업데이트된 전국 N개 정책" — 장식이 아니라 실데이터를 노출하는 것이 신뢰 디자인이다.
4. **관공서 서식이 아니라 대화.** 시스템이 주어인 문장 금지("검색 결과가 없습니다") → 사람이 말 거는 문장("이 방향으론 못 찾았어요. 이런 쪽은 어때요?").
5. **접근성은 KRDS 바닥선.** 공공 데이터 서비스로서 [KRDS](https://www.krds.go.kr/) 접근성 기준(대비·포커스·라벨)을 바닥선으로 참고한다. 단 시각 아이덴티티는 KRDS를 따르지 않는다(관공서 톤 회귀 금지).

## 2. 색 토큰

Tailwind 설정에 아래 이름으로 등록하고, 컴포넌트는 토큰 이름만 사용한다(hex 직접 사용 금지).

### 바탕·텍스트 (웜 뉴트럴)
| 토큰 | hex | 용도 |
|---|---|---|
| `cream-50` | `#FAF6EF` | 페이지 배경 |
| `cream-100` | `#F5EFE4` | 안쪽 면(로드맵 박스 등) |
| `sand-200` | `#E8E0D3` | 헤어라인 보더 |
| `sand-400` | `#B3AA97` | 플레이스홀더·최약 텍스트 |
| `sand-500` | `#8A8272` | 뮤트 텍스트(보조 설명) |
| `sand-600` | `#6B6558` | 세컨더리 텍스트 |
| `ink-800` | `#4A453C` | 본문 보조 |
| `ink-900` | `#2C2A26` | 본문·제목 |
| surface | `#FFFFFF` | 카드·입력면 |

### 포인트 (클레이)
| 토큰 | hex | 용도 |
|---|---|---|
| `clay-500` | `#D85A30` | 주 버튼·핵심 아이콘. **한 화면에 클레이 채움 버튼은 1개만** |
| `clay-050` | `#FAECE7` | 클레이 위 텍스트/옅은 강조면("오늘은 이것만" 박스 배경) |
| `clay-700` | `#993C1D` / `#712B13` | 옅은 클레이 면 위 텍스트(제목/본문 2단계) |

### 상태 배지 (자격 판정 3종 + 위기)
| 상태 | 라벨(카피 가이드 §5) | 배경 | 텍스트 |
|---|---|---|---|
| now | 지금 바로 신청돼요 | `#E1F5EE` (teal-50) | `#085041` (teal-800) |
| soon | 곧 신청이 열려요 | `#E6F1FB` (blue-50, 정보성 소배지 한정) | `#0C447C` (blue-800) |
| review(경미) | 거의 다 왔어요 — N만 확인 | `#FAEEDA` (amber-50) | `#854F0B` (amber-600) |
| review(다수) | 몇 가지만 확인하면 돼요 | `#F1EFE8` (warmgray-50) | `#444441` (warmgray-800) |
| 위기 배너 | (변경 금지 — 기존 유지) | 기존 | 기존 |

규칙: 색 위 텍스트는 반드시 같은 계열의 진한 단계(검정·회색 금지). blocked(빨강)는 카드로 노출하지 않으므로 배지 없음(기존 불변식).

## 3. 모양·타이포

- **라운드**: 카드 16~20px, 입력면 16px, 버튼·알약·배지 pill(999px). 예시 칩은 말풍선 꼬리(`border-radius: 999px 999px 999px 4px`).
- **보더**: 0.5~1px `sand-200` 헤어라인. 그림자·그라데이션 금지(플랫).
- **폰트**: Pretendard(웹폰트) 권장, 폴백 시스템 스택. 두께는 **400/500(또는 600) 2단계만**.
- **크기**: 제목 20~21px·500, 본문 14~15px·400, 보조 12~13px, 최소 11px.
- **간격**: 섹션 간 20~24px, 카드 내부 패딩 16~20px. 여백이 다정함의 절반이다.
- **모션**: 페이드·높이 전환 150~200ms 정도만. 바운스·스프링 금지(원칙 1).

## 4. 컴포넌트 인벤토리

| 컴포넌트 | 파일 | 이 문서 기준 형태 | 비고 |
|---|---|---|---|
| 헤더 인사 | FunnelContainer | "요즘 어때" + 보조문 | §5 카피 |
| 프로필 알약 | ProfileInput | "부산 · 25세 ✏️" 알약 1개, 탭 시 시·도/나이 펼침 | 서식 2칸 노출 금지 |
| 자유입력 | FreeTextInput | 흰 면·16px 라운드, placeholder는 실제 예문 | §5 |
| 예시 칩 | ChoiceChips | 말풍선(꼬리) + 카테고리 라인 아이콘 + 라벨 "이렇게 적는 분들이 많아요" | 아이콘: 하트·집·서류가방·책·지갑 |
| 결과 카드 | PolicyResultCard | 제목 + 상태 배지 + **혜택 한 줄**(D-②) + **나와 맞는 점 체크리스트**(D-①: ✓ teal / ? amber) + 신선도·원문 | 혜택 한 줄 = 그라운딩 통과 LLM 문장만(없으면 raw 요약 폴백, 둘 다 없으면 생략). 자격단정 금지 |
| 카드 펼침(동행) | (Phase F) | 신청 3단계 + "오늘은 이것만"(clay-050 박스) + 신청 페이지 열기(clay) · 내 신청함 저장(아웃라인) + 브리지 문구 + 청년센터 연결 | F-①~⑤ |
| 위기 배너 | SafetyBanner | **변경 금지** | 원칙 2 |
| 위기 푸터 | CrisisFooter | **변경 금지** | 원칙 2 |
| 추정 고지 | DisclaimerNote | 문구 유지, 시각만 §2 뉴트럴 | 원칙 2 |
| 로딩 | (신규) | "2,633개 정책을 뒤지는 중…" (건수는 실데이터) | 원칙 3 |

## 5. 카피 가이드 (확정 문구)

| 위치 | 기존 | 확정 |
|---|---|---|
| 헤더 | 청년정책 진단 | **요즘 어때** |
| 헤더 보조 | 마음이 어떤지 적어보면… | 지금 상황을 편하게 적어주면, 맞는 정책을 찾아드려요 |
| placeholder | 마음을 글로 적어볼까요? | 자취하는데 월세가 벅차요… (실제 예문) |
| 버튼 | 정책 찾기 | **내 정책 찾기** |
| 결과 헤드라인 | '…' 추정 결과 | 상황에 맞을 만한 **N개**를 찾았어요 |
| 빈 결과 | 지금 바로 맞는 정책을 찾지 못했어요. | 이 방향으론 못 찾았어요. 이런 쪽은 어때요? |
| 배지 now | 지금 신청 가능 | 지금 바로 신청돼요 |
| 배지 soon | 곧 시작/마감 | 곧 신청이 열려요 |
| 배지 review(경미) | 거의 충족 | 거의 다 왔어요 — N만 확인 |
| 배지 review | 자격 확인 필요 | 몇 가지만 확인하면 돼요 |
| 원문 버튼 | 원문 보기 | **신청 페이지 열기 (출처)** — 출처는 `policy.source`로 분기: 온통청년→"(온통청년)", 서울 청년몽땅→"(청년몽땅)", 불명→괄호 생략(거짓 출처 표기 금지) |
| 브리지(F-①) | (없음) | 열리는 페이지에서 '신청하기' 버튼을 찾으면 돼요 |
| 신선도 | (없음) | 어제 업데이트된 전국 N개 정책에서 찾아드려요 |
| 동행(F-③) | (없음) | 혼자 하기 버거우면 OO 청년센터가 같이 해줘요 (검증된 연락처 있을 때만 블록 노출) |

**금지어·금지 패턴**: 시스템 주어("~할 수 없습니다"), 느낌표 남발, "간단히/쉽게"(무기력 사용자에게 압박), 자격 단정("자격이 됩니다/안 됩니다" — 안전 불변). 응원 카피는 절제(체크 1회당 1줄 이내, 과잉 금지).

## 6. 접근성 바닥선 (KRDS 참조)

- 텍스트 대비 4.5:1 이상(뮤트 텍스트도 큰 글자 기준 3:1 이상), 색만으로 상태 구분 금지(배지에 텍스트 라벨 필수).
- 모든 입력에 `<label>` 연결(기존 관용 유지), 포커스 링 가시(2px, clay 또는 기존 sky→clay 전환).
- 터치 타깃 최소 44×44px(칩·버튼·알약).
- 키보드만으로 전 흐름 조작 가능(Enter 전송·Shift+Enter 줄바꿈 유지).

## 7. 안전 불변 조항 (UI 작업 시 절대 규칙)

1. 위기 상태 → SafetyBanner 단독 렌더(입력·결과·예시·프로필·동행 UI 전부 미렌더). 어떤 리디자인도 이 불변식을 못 건드린다.
2. "추정" 고지·원문 링크·신선도는 모든 결과 카드에 유지.
3. 상태 라벨 문구 변경 시 **기존 테스트의 라벨 단언 전면 갱신 동반**(라벨은 test/integration/funnel/* 에서 단언됨).
4. 자격 단정으로 읽히는 표현 금지 — 체크리스트 ✓는 "충족/제한 없음" 사실 서술로만(자격 단정 금지). '추정' 성격은 DisclaimerNote가 1회 고지(항목별 "(추정)" 중복 표기 안 함). 소득 pass는 무관 정책이므로 "소득 제한 없음".
5. UI 변경은 safety-domain-auditor 감사 대상(안전 표면 접촉 시).

## 8. 운영

- 이 문서가 곧 스펙이다. 시안(Figma/이미지)은 참고물, 충돌 시 이 문서 우선.
- 새 화면·컴포넌트를 만들면 §4 인벤토리에 한 줄 추가.
- Figma는 "보여주는 곳"(발표 자산·시안 변형 탐색)으로만 쓴다. Figma → 코드 반영 시에도 이 문서를 경유(토큰·카피 확정 후 구현).
